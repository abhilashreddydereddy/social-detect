# Minimal set to run the API with the heuristic detectors only (no torch/
# transformers). The CLIP semantic detector will automatically report
# `available: false` and be skipped by the registry -- everything else works.
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.4.0
scipy==1.14.1
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
pytest==8.3.3
pytest-asyncio==0.24.0
